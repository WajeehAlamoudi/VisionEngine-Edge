models:
  - id: ""
    name: ""
    version: "1.0.0"
    path: ""                  # ./models/yolov8n.pt | absolute path
    device: auto              # auto | cpu | cuda | hailo | coreml | mps
    classes: []               # full list of classes this model can detect
    confidence_threshold: 0.5
    iou_threshold: 0.45
    input_size: [640, 640]
    use_tracker: false        # true = BoT-SORT tracker ON → track_id populated per object
