collection:
  - output_dir: "./collected"

  # uncomment and fill to enable a collection session
  # - id: ""
  #   camera: ""              # must match an id in cameras.yaml
  #   enabled: false
  #   schedule:
  #     after: ""             # HH:MM — start time each day
  #     before: ""            # HH:MM — end time each day
  #     start_date: ""        # YYYY-MM-DD
  #     end_date: ""          # YYYY-MM-DD
  #   sampling:
  #     mode: interval        # interval | on_detection | random
  #     interval_seconds: 10
  #     frames_per_minute: 1
  #   filters:
  #     classes: []
  #     min_confidence: 0.5
  #     min_detections: 1
  #   save:
  #     annotated: true
  #     raw: true
  #     metadata: true
  #   max_frames: 0
